# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/

# Sensitive data — never commit real credentials
config.yaml

# Browser data (persistent login sessions, not for CI)
browser_data/

# Logs
logs/
*.log

# Data directory — listings.db IS tracked for CI state persistence
# (allows deduplication across scheduled runs without external storage)
data/*
!data/.gitkeep
!data/listings.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.bak
get_chat_id.py
get_chat_id_auto.py
